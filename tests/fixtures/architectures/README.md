# Structural architecture fixtures

These are deliberately tiny synthetic configs, not downloaded model artifacts or
upstream defaults. They pin the inspector's structural examples without weights.
Source documentation for each family is recorded in `ARCHITECTURE_RECIPES`.
No fixture demonstrates model loading, numerical correctness or GPU performance.
Upstream commit-pinned config inventories remain a separate A0 task.
